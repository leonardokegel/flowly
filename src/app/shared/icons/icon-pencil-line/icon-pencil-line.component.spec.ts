import { IconPencilLineComponent } from './icon-pencil-line.component';

describe('MapPinLineComponent', () => {
  const component: IconPencilLineComponent = new IconPencilLineComponent();

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
