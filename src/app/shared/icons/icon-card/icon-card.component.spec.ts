import { IconCardComponent } from './icon-card.component';

describe('IconCardComponent', () => {
  const component: IconCardComponent = new IconCardComponent();

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});