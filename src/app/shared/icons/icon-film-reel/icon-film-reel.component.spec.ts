import { IconFilmReelComponent } from './icon-filme-reel.component';

describe('IconFilmReelComponent', () => {
  const component: IconFilmReelComponent = new IconFilmReelComponent();

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
