import { IconTrashComponent } from './icon-trash.component';

describe('IconTrashComponent', () => {
  const component: IconTrashComponent = new IconTrashComponent();

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
