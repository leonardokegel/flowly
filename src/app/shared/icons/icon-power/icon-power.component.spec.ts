import { IconPowerComponent } from './icon-power.component';

describe('MapPinLineComponent', () => {
  const component: IconPowerComponent = new IconPowerComponent();

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
