import { IconPlusComponent } from './icon-plus.component';

describe('MapPinLineComponent', () => {
  const component: IconPlusComponent = new IconPlusComponent();

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
