import { IconBedComponent } from './icon-bed.component';

describe('IconBedComponent', () => {
  const component: IconBedComponent = new IconBedComponent();

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
