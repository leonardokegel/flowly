import { IconChatComponent } from './icon-chat.component';

describe('IconChatComponent', () => {
  const component: IconChatComponent = new IconChatComponent();

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
