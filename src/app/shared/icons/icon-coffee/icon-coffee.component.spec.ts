import { IconCoffeeComponent } from './icon-coffee.component';

describe('MapPinLineComponent', () => {
  const component: IconCoffeeComponent = new IconCoffeeComponent();

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
