import { PageLayoutComponent } from './page-layout.component';

describe('PageLayoutComponent', () => {
  let component: PageLayoutComponent;

  beforeEach(async () => {
    component = new PageLayoutComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
