export interface IDadosSessaoState {
  id: string;
  nome: string;
  email: string;
  token: string;
}

export interface IDadosClientesState {
  id: string;
  userId: string;
  empresa: string;
  nome: string;
  email: string;
}
